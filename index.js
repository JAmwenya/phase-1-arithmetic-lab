// equation that will multiply the variables num1 and num2
const num1 = 31;
const num2 = 2;
const multiply = num1 * num2;


// Generate a random integer greater than 0
const random = Math.floor(Math.random() * 5) + 1;


// equation that will calculate the remainder of dividing variable num3 by num4
const num3 = 9;
const num4 = 5;
const mod = num3 % num4;


// finds the highest number in a set; the value returned should be 20
const max = Math.max(3, 4, 8, 15, 20);

console.log(max);